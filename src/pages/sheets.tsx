import React from 'react'

import Layout from '../components/layout'

const SheetsPage = () => {
  return (
    <Layout headProps={{ title: 'SHEETS', description: '東方のリコーダーアンサンブルの楽譜等を置いています。' }}>
    </Layout>
  )
}
